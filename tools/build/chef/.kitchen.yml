driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-12.04

suites:
  - name: netcommons
    run_list:
      - recipe[netcommons]
    attributes:
      boilerplate:
        country: 'jp'
        git:
          use_git_protocol: false
